# Admin secret key for user management endpoints
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
ADMIN_API_KEY=your-admin-secret-key-here

# SQLite database path (default: licence_disc.db in working directory)
DATABASE_PATH=licence_disc.db

# Server config
PORT=8080
FLASK_DEBUG=0
